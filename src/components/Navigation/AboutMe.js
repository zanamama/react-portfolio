import "./header.css";
import Navigation from "./navigation";

const Header = () => {
  return (
    <header>
      <Logo></Logo>
      <Navigation></Navigation>
    </header>
  );
};

export default Header;
